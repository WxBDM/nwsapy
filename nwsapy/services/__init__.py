"""This package acts as an interface to modify data throughout the package.
It provides functionality that is "layered" on top of the ``core`` package,
so that developers for NWSAPy never have to edit the ``core`` package
to introduce new functionalities to the package.
"""